{
  "name": "featurehub-webhook-utils",
  "version": "1.0.0",
  "description": "Base utils to allow the webhook to be turned into the SDK payload",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && node ./node_modules/typescript/bin/tsc",
    "link": "node ./node_modules/typescript/bin/tsc && npm link",
    "lint": "./node_modules/.bin/eslint . --ext .js,.jsx,.ts,.tsx --fix"
  },
  "author": "featurehub.io",
  "dependencies": {
    "featurehub-javascript-webhooks": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/chai": "^4.3.10",

    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "@types/mocha": "^9.0.0",
    "typescript": "^5.2.2"
  }
}
