{
  "name": "featurehub-webhook-aws",
  "version": "1.0.0",
  "description": "Provides functionality for AWS support for a webhook",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && node ./node_modules/typescript/bin/tsc",
    "setup": "npm link featurehub-webhook-utils",
    "link": "npm run build && npm link",
    "lint": "./node_modules/.bin/eslint . --ext .js,.jsx,.ts,.tsx --fix"
  },
  "author": "featurehub.io",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.454.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "@types/mocha": "^9.0.0",
    "typescript": "^5.2.2"
  }
}
